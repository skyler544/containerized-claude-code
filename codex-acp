#!/usr/bin/env bash
# Run the Codex ACP adapter in the same container and workspace as Codex CLI.

set -euo pipefail

SCRIPT_PATH="$(readlink -f "${BASH_SOURCE[0]}")"
SCRIPT_DIR="$(cd -- "$(dirname -- "$SCRIPT_PATH")" && pwd)"
exec "$SCRIPT_DIR/run-codex-container" codex-acp "$@"
