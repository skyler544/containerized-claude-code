#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec podman build --no-cache \
	--tag codex-cli:latest \
	--file "$SCRIPT_DIR/Dockerfile" \
	"$SCRIPT_DIR"
